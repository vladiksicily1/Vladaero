[package]
name = "vladfs"
version = "0.1.0"
edition = "2021"
description = "VladFS — Structured modern filesystem for VladOS inspired by NTFS MFT"

[dependencies]

[[bin]]
name = "mkfs_vladfs"
path = "src/bin/mkfs.rs"
required-features = ["std"]

[features]
default = ["std"]
std = []
