[package]
name = "gdi32"
version = "0.1.0"
edition = "2021"

[lib]
name = "gdi32"
crate-type = ["staticlib"]

[dependencies]
ntdll = { path = "../ntdll" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 2
lto = true
