[package]
name = "vlados-kernel"
version = "0.1.0"
edition = "2021"

[lib]
name = "vlados_kernel"
path = "src/lib.rs"
crate-type = ["lib"]

[profile.dev]
panic = "abort"
opt-level = 0

[profile.release]
panic = "abort"
opt-level = 2
lto = true
